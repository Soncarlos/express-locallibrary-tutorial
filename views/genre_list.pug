extends layout 

block content 

   h1  #{title}

   ul 
      each genre in genre_list
         li    
            a(href= genre.url) #{genre.name}            
            a(href=genre.url+'/delete') Delete 
            a(href= genre.url+'/update') Update
      else 
         li There are no Genre in DataBase        